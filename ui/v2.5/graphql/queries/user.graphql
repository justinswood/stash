query Me {
  me {
    ...UserData
    # Fetched here rather than in the UserData fragment so that findUsers does
    # not resolve capabilities per row (each one reads that user's overrides).
    capabilities
  }
}

query FindUsers {
  findUsers {
    ...UserData
  }
}

query FindUser($id: ID!) {
  findUser(id: $id) {
    ...UserData
  }
}

query FindUserAPIKeys($user_id: ID) {
  findUserAPIKeys(user_id: $user_id) {
    ...UserAPIKeyData
  }
}

query AllCapabilities {
  allCapabilities
}

query FindUserCapabilities($id: ID!) {
  findUser(id: $id) {
    ...UserData
    ...UserCapabilityData
  }
}
